machine:
  java:
    version: oraclejdk8
dependencies:
  override:
    - mvn clean install -DskipTests
  cache_directories:
    - ~/.m2

general:
  artifacts:
    - "c5-general-replication/target/surefire-reports"
